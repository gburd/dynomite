[package]
name = "mapreduce-double"
version = "0.0.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
ciborium = "0.2"
serde_json = "1"

[profile.release]
opt-level = "s"
lto = true
panic = "abort"

[workspace]
