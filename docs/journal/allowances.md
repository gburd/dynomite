# Allowances

Each `#[allow(...)]` or `#![allow(...)]` in the codebase requires a row
here, citing the exact lint name, the file/line, the upstream
issue/limitation, and the date.

| Date | Crate | File | Lint | Reason |
|---|---|---|---|---|
| 2026-05-20 | dynomite | crates/dynomite/src/stats/numeric.rs | clippy::cast_precision_loss, clippy::cast_possible_truncation, clippy::cast_sign_loss | The `floor_p_times_u64` helper must match the reference percentile expression `floor((double)scale * percentile)` exactly. The function performs the multiplication in IEEE 754 `f64` and floors the product. Any alternate arithmetic that avoids the casts diverges from the reference at percentile cutoffs the histogram actually uses (for example `p=0.95` over `scale=1000` yields 950 in the reference and 949 with rational arithmetic). The casts are protected by explicit range and finiteness checks before the conversion. |
| 2026-05-20 | dynomite | crates/dynomite/src/stats/numeric.rs | clippy::cast_precision_loss, clippy::cast_possible_truncation, clippy::cast_sign_loss (test) | The accompanying unit test `floor_matches_f64_reference_over_known_pairs` reproduces the reference expression literally. |
| 2026-05-19 | dynomite | `crates/dynomite/src/util/time.rs` | `clippy::cast_possible_truncation` | The `u128 -> u64` step in `usec_now` runs only after a guard rejects values larger than `u64::MAX`, so truncation is unreachable. |
| 2026-05-19 | dynomite | crates/dynomite/src/hashkit/mod.rs | `non_camel_case_types` | The C reference exports the algorithm names `fnv1_64`, `fnv1a_64`, `fnv1_32`, `fnv1a_32` and the configuration parser keys on those exact strings. Renaming the variants to UpperCamelCase would either obscure the parity with the spec or require a separate name table, both of which lose information. The lint is allowed only on the `HashType` enum. |
| 2026-05-19 | workspace | Cargo.toml | `clippy::doc_markdown` | Trade names `MurmurHash`, `SuperFastHash`, `MD5` etc. trigger this lint. Adding backticks would make doc text read as code in rustdoc. Workspace-scope is appropriate because trade names appear broadly in module docs. |
| 2026-05-20 | dynomite | crates/dynomite/src/hashkit/mod.rs | `clippy::cast_possible_truncation`, `cast_possible_wrap`, `cast_sign_loss` | Hashkit dispatch and per-algorithm modules truncate, wrap, and reinterpret integer types as part of the bit-mixing contract. Module-scope `#![allow]` keeps the latitude bounded to the hashkit subtree; future stages must justify their own casts locally. |
| 2026-05-20 | dynomite | crates/dynomite/src/hashkit/ketama.rs | `clippy::cast_precision_loss` | Weighted-fraction arithmetic widens u32 weights into f64 to mirror the C continuum builder; precision loss at u32::MAX is negligible for ring-point counts. Module-scope. |
| 2026-05-20 | dynomite | crates/dynomite/src/hashkit/md5.rs | `clippy::many_single_char_names`, `similar_names` | RFC 1321 names the round registers `a`, `b`, `c`, `d`. Preserving the short names keeps the round table directly verifiable against the spec. Module-scope. |
| 2026-05-20 | dynomite | crates/dynomite/src/hashkit/jenkins.rs | `clippy::many_single_char_names`, `similar_names` | The Bob Jenkins lookup3 reference uses working registers `a`, `b`, `c`. Same rationale as MD5. Module-scope. |
| 2026-05-20 | dynomite | crates/dynomite/src/hashkit/md5.rs (`step`) | `clippy::too_many_arguments` | The MD5 round step takes its mixing function plus the four registers and the round constants by value to keep the body of `body()` a flat transliteration of the RFC 1321 round table. Per-function. |
| 2026-05-20 | dynomite | crates/dynomite/src/hashkit/murmur3.rs (`murmur3_x86_128`) | `clippy::too_many_lines` | The contrib MurmurHash3 x86_128 implementation is one straight-line state machine with a 15-case tail fallthrough; splitting it across helpers would obscure the parity with the upstream C. Per-function. |
| 2026-05-19 | dynomited | crates/dynomited/src/cli.rs (`Cli`) | `clippy::struct_excessive_bools` | The CLI struct preserves the reference engine's getopt_long flag set; six independent boolean toggles (show_help, show_version, test_conf, daemonize, describe_stats, gossip) all map 1:1 to the C `getopt` arms. Collapsing them changes the parser surface. Per-struct. |
| 2026-05-19 | dynomited | crates/dynomited/Cargo.toml + crates/dynomited/src/daemonize.rs | `unsafe_code` (workspace `forbid` -> crate `deny` + module `allow`) | The reference engine's `dn_daemonize` performs the canonical UNIX double-fork sequence (fork, setsid, fork, chdir, umask, dup2). `nix::unistd::fork` is `unsafe` because the post-fork child is restricted to async-signal-safe calls. The Cargo manifest drops the lint level to `deny`, the `daemonize` module re-allows it, and the safety invariants (called from `main` before tokio, before signal handlers, before any worker threads) are documented in the module rustdoc. The unsafe surface is two `fork()` calls; everything else uses safe `nix` wrappers. Per-module.
| 2026-05-20 | dyn-hash-tool | crates/dyn-hash-tool/src/main.rs (`Cli`) | `clippy::struct_excessive_bools` | The CLI struct mirrors the C `dyn_hash_tool` flag set; the booleans (`stdin`, `list`, `c_compat`, `c_outputkey`) are independent toggles, not state-machine inputs. Per-struct. |
| 2026-05-19 | dynomite | crates/dynomite/src/msg/message.rs (`MsgFlags`) | `clippy::struct_excessive_bools` | The flag bag preserves the `unsigned :1` bit field on `struct msg` from the reference engine; thirteen independent toggles (error, fragment-error, quit, swallow, done, fdone, dnode-header-prepended, rsp-sent, expect-datastore-reply, is-read, needs-repair, rewrite-with-ts-possible, rspmgrs-inited) are read and written independently across the request and response paths. Per-struct. |
| 2026-05-19 | dynomite | crates/dynomite/src/proto/dnode.rs | `clippy::cast_possible_truncation` | The DNODE parser truncates ASCII-decimal accumulators into the same fixed bit widths the reference engine writes on the wire (`uint8_t` for the type and flags fields, `uint32_t` for the data and payload lengths). Replacing the casts with checked conversions changes the observable parser behaviour on malformed inputs and breaks parity with `_/dynomite/src/dyn_dnode_msg.c`. Module-scope. |
| 2026-05-19 | dynomite | crates/dynomite/src/proto/dnode.rs (`DnodeParser::step`) | `clippy::too_many_lines` | The parser is the Rust counterpart to `dyn_parse_core` in the reference engine, which is one large switch statement; AGENTS.md Section 5 explicitly directs that long state machines stay in a single function so the parity walk is mechanical. Per-function. |
| 2026-05-19 | dynomite | crates/dynomite/src/proto/redis/parser.rs | `clippy::cast_possible_truncation`, `cast_possible_wrap`, `too_many_arguments`, `match_same_arms`, `manual_let_else` | Module-scoped: the Redis (RESP) parser is the single largest state machine in the project (`redis_parse_req` is ~1500 LOC of C). AGENTS.md Section 5 directs that long state machines stay in a single function; the per-state arms repeat the same recovery shape (token bookkeeping plus `finish_req_error`) which triggers `match_same_arms` and `manual_let_else`. The width casts mirror the C `(uint32_t)` accumulators on the wire. Module-scope. |
| 2026-05-19 | dynomite | crates/dynomite/src/proto/redis/commands.rs | `clippy::too_many_lines`, `match_same_arms` | Module-scoped: `lookup` (the keyword-to-`MsgType` table) and `classify` (the argument-shape catalogue) are flat dispatch tables that mirror the reference engine's per-command switches. Splitting them across helpers obscures the side-by-side parity check. Module-scope. |
| 2026-05-19 | dynomite | crates/dynomite/src/proto/redis/fragment.rs | `clippy::cast_possible_truncation` | The fragmenter assigns each input key to a shard index returned by the dispatcher (a `u32` by contract); the per-fragment shard index is therefore safe to truncate from `usize`. Module-scope. |
| 2026-05-19 | dynomite | crates/dynomite/src/proto/memcache/parser.rs | `clippy::cast_possible_truncation`, `too_many_arguments`, `match_same_arms`, `manual_let_else`, `redundant_else` | Same rationale as `proto::redis::parser` (state-machine port); the Memcached parser is the smaller cousin but inherits the same shape requirements. Module-scope. |
| 2026-05-19 | dynomite | crates/dynomite/src/proto/memcache/fragment.rs | `clippy::cast_possible_truncation` | Same rationale as `proto::redis::fragment`. Module-scope. |
| 2026-05-19 | dynomite | crates/dynomite/tests/stage_08_proto.rs | `clippy::too_many_lines`, `unexpected_cfgs` | The test file holds two large hand-curated input tables (50+ Redis commands, 12+ Memcache commands); splitting them across helpers loses the at-a-glance row layout. The `c-diff` cfg is reserved for the differential test that lights up once Stage 0 ships a static-lib build of the C parser; that build artifact is tracked in the Stage 8 journal. Per-file. |
| 2026-05-19 | dynomite | crates/dynomite/src/net/conn.rs (`Conn`) | `clippy::struct_excessive_bools` | The connection struct preserves the `unsigned :1` bit field on the C reference `struct conn` (`eof`, `done`, `same_dc`, `dyn_mode`, `dnode_secured`, `crypto_key_sent`); the booleans are independent lifecycle toggles read and written across role-specific drivers. Per-struct. |
| 2026-05-19 | dynomite | crates/dynomite/src/net/quic.rs (`spawn_driver`) | `clippy::too_many_lines` | The QUIC driver hosts the single tokio task that owns a `quiche::Connection`, the UDP socket, and the per-stream channels. Splitting the select! body across helpers introduces extra await points that change the loop's atomicity guarantees. Per-function. |
| 2026-05-19 | dynomite | crates/dynomite/src/cluster/peer.rs (`Peer::new`) | `clippy::too_many_arguments` | The C reference initialises a peer through `_init_peer_struct` plus the per-field assignments in `dnode_initialize_peer_each` (8+ independent fields: index, endpoint, rack, dc, tokens, is_local, is_same_dc, is_secure). Collapsing them into a builder loses the parity with the C field-set; collapsing them into a tuple loses the doctest legibility. Per-function. |
| 2026-05-19 | dynomite | crates/dynomite/src/proto/dnode.rs, memcache/parser.rs, redis/parser.rs, stats/rest.rs | `clippy::needless_continue` | The state-machine parsers and the HTTP read loop carry explicit `continue;` statements that mirror the C reference's loop-restart structure. Removing them is mechanical but changes the parity with `dyn_parse_core`, `memcache_parse_*`, and `redis_parse_*` and obscures the per-state walk. Module-scope per file. |
| 2026-05-19 | dynomite | crates/dynomite/tests/conf_property.rs, stage_06_crypto.rs, stage_09_quic.rs, examples/generate_hashkit_vectors.rs | `clippy::format_push_string`, `clippy::needless_continue`, `clippy::unnecessary_debug_formatting`, `clippy::match_same_arms`, `clippy::similar_names`, `clippy::cast_possible_truncation` | Test fixtures and the corpus generator emit human-readable diagnostic output; rewriting them to satisfy Rust 1.90's stricter pedantic suite reduces test legibility without changing the assertions. Per-file. |
| 2026-05-19 | dynomited | crates/dynomited/tests/conformance/mod.rs | `dead_code`, `missing_docs` | The Stage 14 conformance helper module exposes a cluster spawner, RESP client, and supporting types as `pub` items so per-scenario test files can `use crate::helpers::*;`. Some helpers are only consumed by a subset of scenarios (so `dead_code` fires when other scenarios are skipped via runtime checks), and the `pub` visibility triggers `missing_docs` on internal struct fields whose semantics are obvious from context (process handles, ports, paths). Module-scope. |
| 2026-05-20 | dynomite | crates/dynomite/benches/*.rs | `missing_docs` | Each criterion bench file expands the `criterion_group!` and `criterion_main!` macros, both of which generate public functions (`benches`, `main`) without rustdoc. The bench files are not part of the public API and are exercised exclusively through `cargo bench`; documenting the macro-generated symbols is impossible without forking the criterion macros. Per-file. |
| 2026-05-20 | dynomite-fuzz | crates/fuzz/fuzz_targets/*.rs | `unsafe_code` (workspace `forbid` -> crate-local `allow`) | The `libfuzzer_sys::fuzz_target!` macro expands to `extern "C" fn LLVMFuzzerTestOneInput(...)` plus an `unsafe` slice reconstruction from raw libfuzzer C buffers; this is the documented contract between the libfuzzer C entry points and the Rust harness body. The fuzz crate is excluded from the workspace and only the `fuzz_target!`-expanded code is `unsafe`; the library code under test still lives behind `#![forbid(unsafe_code)]`. Per-crate. |
