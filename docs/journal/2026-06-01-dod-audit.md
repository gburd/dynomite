# v0.1.0 Definition-of-Done Audit (2026-06-01)

Run from repo HEAD ab64849 on main. Auditing each of the 14
items in AGENTS.md Section 14.

## Summary table

| # | Item | Status |
|---|---|---|
| 1 | PLAN.md Stage 16 exit gates | partial -- see #7 |
| 2 | docs/parity.md every C symbol mapped | OK (2371 lines; full diff audit not re-run this pass) |
| 3 | Stage 14 differential test for both transports | TCP done; QUIC not exercised under chaos |
| 4 | Stage 15 fuzz soak >=1h per target | NOT MET (7 targets exist; ran 60s smoke only) |
| 5 | Coverage >=95% line/branch/function | PARTIAL -- see Deviations below |
| 6 | Stage 15 macro benches with committed baselines | OK (8 baselines under crates/dynomite/benches/baseline/) |
| 7 | Stage 16 chaos: 1h zero violations + clean post-test sweep | NOT MET (chaos passes 1-9 ran; none claim zero violations) |
| 8 | mdbook clean + embedding examples compile | UNVERIFIED (mdbook not in local PATH) |
| 9 | cargo public-api stable for 2 PRs | UNVERIFIED (cargo-public-api not installed) |
| 10 | Python conformance ported to Rust | OK (under crates/dynomited/tests/conformance/) |
| 11 | GitHub + Codeberg CI green on same commit | RECOVERED (ab64849 should be green; latest forgejo TBD) |
| 12 | All commits authored as `Greg Burd <greg@burd.me>` | NOT MET (10+ historical worker authors) |
| 13 | Signed v0.1.0 tag | NOT MET (tag does not exist; signing config broken at v0.0.1) |
| 14 | Fresh checkout / scripts/check.sh end-to-end | OK (rc=0 just now) |

## Coverage Deviations (for #5)

The cargo-llvm-cov pass on commit ab64849 reported these
sub-95% files. Per AGENTS.md #5 each requires a Deviation
entry in docs/parity.md or a coverage-improvement task.

| crate | file | line% | branch% | function% | rationale |
|---|---|---:|---:|---:|---|
| dynomited | noxu_backend.rs | 92.3 | 90.7 | 100.0 | error paths to disk-full / IO unreachable in unit tests |
| dynomited | observability.rs | 78.1 | 73.5 | 75.9 | OTLP exporter setup paths gated on env vars not present in CI |
| dynomited | reload.rs | 78.3 | 86.7 | 94.1 | SIGHUP-driven reload paths exercised via a separate integration binary |
| dynomited | riak.rs | 90.2 | 88.0 | 90.0 | error-classification paths for malformed PBC frames |
| gen-fsm | action.rs | 34.1 | 20.8 | 41.7 | trait default-impl methods only invoked via macro callers in dependents |
| gen-fsm | driver.rs | 70.4 | 66.5 | 81.8 | error-recovery branches for wedged-handler scenarios |
| gen-fsm | handler.rs | 50.0 | 50.0 | 50.0 | default-impl methods on the Handler trait |
| gen-fsm | transition.rs | 0.0 | 0.0 | 0.0 | type alias + Display impl; cargo-llvm-cov does not instrument |
| loom-tests | lib.rs | 0.0 | 0.0 | 0.0 | loom test entry points only run under `RUSTFLAGS="--cfg loom"` |
| throttle-core | lib.rs | 90.8 | 92.5 | 89.7 | clock-edge cases exercised by the loom suite, not unit tests |
| tre-sys | lib.rs | 90.5 | 87.0 | 83.3 | FFI shim with platform-specific error returns hard to drive |

These deviations are pinned in `docs/parity.md` under the
"Coverage deviations" section.

## Outstanding work for v0.1.0

In rough priority order:

1. **#7 chaos hour** -- a clean 1h pass-9-style run with
   process+network+clock+disk fault classes and a verified
   post-test sweep producing zero invariant violations.

2. **#12 commit author normalisation** -- `git filter-repo`
   to rewrite all author/committer to `Greg Burd <greg@burd.me>`.
   Done as the LAST operation before the v0.1.0 tag.

3. **#13 signed tag** -- fix the SSH-signing config (was the
   error at v0.0.1) and signed-tag v0.1.0.

4. **#3 QUIC differential** -- run the conformance differential
   suite over the QUIC transport (today only TCP runs).

5. **#4 fuzz soak** -- 1h per target * 7 targets = 7h. CI
   weekly job slot.

6. **#5 coverage** -- either raise sub-95% files or pin them
   as documented Deviations (this entry counts).

7. **#8 mdbook + #9 cargo public-api** -- install both, run,
   commit baselines, run twice across consecutive PRs to
   validate stable surface.

8. **post-chaos-queue.md remaining items** (orthogonal to DoD
   but should be cleared):

   - #1 Redis AUTH support
   - #2 Pidfile flock race
   - P3-2.5 Gossip-driven peer-state oscillation metrics
   - P3-3.8 network-partition + clock-skew + disk-full chaos
     modes (subsumed under DoD #7)
   - P3-3.9 phase 5 differential chaos (process-fault parity
     between Rust + C proxies)

## Items confirmed DONE

- #6: criterion baselines under `crates/dynomite/benches/baseline/`
  (parsers, tokens, hashkit, mbuf, crypto, dnode, quorum)
- #10: python conformance scenarios re-implemented at
  `crates/dynomited/tests/conformance/` (single_node,
  three_node_single_dc, multi_dc, quic_transport,
  python_harness)
- #14: scripts/check.sh runs to rc=0 end-to-end on a fresh
  workspace

