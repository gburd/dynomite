# 2026-05-23 noxu workspace dependency bump (no-op verification)

## Task

Refresh the `noxu-*` path-dep closure in `Cargo.toml` against the
latest upstream noxu tree, verify no API drift, and confirm all CI
gates remain green with and without the `riak-storage` feature.

## Lamdb state

* Repository: `/home/gburd/ws/noxu` (Codeberg
  `ssh://git@codeberg.org/gregburd/noxu.git`).
* Local `main` HEAD: `3bc6a12a077d73d158d6fa02fd824c2521541bfe`
  ("feat(examples): add cash, cask, and ftdb application examples").
* Remote `origin/main` HEAD: `3bc6a12a077d73d158d6fa02fd824c2521541bfe`
  (identical).
* `git fetch` showed no new commits.
* Local working tree was dirty (modified `noxu-rep/quoracle`
  submodule, deleted `.claude/scheduled_tasks.lock`, untracked
  `benches/overnight.sh` and `crates/noxu-rep/core.274039`). Per
  AGENTS.md operator brief, `git pull --ff-only` was skipped on a
  dirty tree; with local already at `origin/main` the pull would
  have been a no-op regardless. None of the dirt is in any crate
  this workspace consumes.
* No file under `/home/gburd/ws/noxu/` was modified.

## Crate inventory diff

The 13 `noxu-*` crates declared in
`[workspace.dependencies]` of `dynomite/Cargo.toml` all still
exist at the documented paths:

```
noxu-cleaner  noxu-config  noxu-db   noxu-dbi    noxu-engine
noxu-evictor  noxu-latch   noxu-log  noxu-recovery
noxu-sync     noxu-tree    noxu-txn  noxu-util
```

New noxu crates that we do **not** depend on (out of scope for
this bump): `noxu-bind`, `noxu-collections`, `noxu-observe`,
`noxu-persist`, `noxu-rep`, `noxu-xa`. None of them are
prerequisites of the closure we already pull in.

## API drift

None. With the noxu tree byte-identical to the prior session
state, no public surface changed and no consumer fix-up is
needed. The `riak-storage` feature still resolves through
`noxu-db` and walks the full closure.

## Gate results (worktree:
`/tmp/pi-agent-72212b5d-f712-46e-1a72d471`, branch
`chore/noxu-bump-2026-05-23`)

| Gate | Result |
|------|--------|
| `cargo build --workspace --all-targets --locked` | clean |
| `cargo build --workspace --features riak-storage --all-targets --locked` | clean (all 13 noxu crates compile) |
| `cargo nextest run --workspace` | 727 passed, 4 skipped (>= 727 threshold met) |
| `cargo clippy --workspace --all-targets --all-features -- -D warnings` | clean |

## Issues filed

None. Every noxu crate we depend on builds cleanly under the
default and `riak-storage` feature sets, and nothing in the
public surface we already use exhibits the kind of obvious bug
(unnecessary `&mut self`, panic-on-edge-case where `Result`
would do, etc.) that AGENTS.md tells us to report upstream.

## Files touched in this tree

Only this journal entry.

## Notes

The path-dep model means a "bump" is purely advisory unless
noxu has moved. We re-ran the gates anyway to keep the audit
trail complete. The next bump should compare against
`LAMDB_NEW_SHA = 3bc6a12a077d73d158d6fa02fd824c2521541bfe` so
the diff is non-empty.
